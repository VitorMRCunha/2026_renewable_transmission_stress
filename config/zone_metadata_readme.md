# Meteorological zones

The six locations are generic mainland-Portuguese meteorological zones used to
calibrate spatial and temporal renewable dependence. They do not provide a
geographical embedding of IEEE 118-bus nodes.

The machine-readable coordinates and zone identifiers are stored in
`weather_calibration/fitted_parameters/zone_metadata.csv`.
